<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="code.namanbir.gitfit.app">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name="code.namanbir.gitfit.app.app.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Dark">
        <activity android:name="code.namanbir.gitfit.app.ui.user.UserActivity"
            android:screenOrientation="portrait"/>
        <activity android:name="code.namanbir.gitfit.app.ui.BattleActivity"
            android:screenOrientation="portrait"/>
        <activity android:name="code.namanbir.gitfit.app.ui.SplashActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>